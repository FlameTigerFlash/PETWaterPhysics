using UnityEngine;

public class Trapezoid
{
    public Vector3 A, B, C, D;

    public Trapezoid(Vector3 a, Vector3 b, Vector3 c, Vector3 d)
    {
        A = a;
        B = b;
        C = c;
        D = d;
    }
}
